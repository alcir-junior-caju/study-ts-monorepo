{
  "name": "monorepo-node-react-native-next",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "author": "Alcir Junior <junior@cajucomunica.com.br>",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "test": "cross-env CI=true jest --bail --findRelatedTests",
    "lint": "eslint --fix packages/backend/src/** && eslint --fix packages/web/src/**",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "yarn lint",
      "yarn test"
    ]
  },
  "devDependencies": {
    "@monorepo/eslint-config": "*",
    "@monorepo/husky-config": "*",
    "@monorepo/lint-staged-config": "*",
    "@types/jest": "^27.0.1",
    "cross-env": "^7.0.3",
    "jest": "^27.2.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
